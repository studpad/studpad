<br>
<br>
<br>
<br>
<br>

<%= form_for current_user, :url => new_ava_user_path(@user), :html => { :multipart => true } do |form| %>
  <%= form.cropbox :avatar, width: 100, height: 100 %>
  <%= form.previewbox :avatar, width: 100, height: 100%>
  <%= form.button :submit %>
<% end %>
