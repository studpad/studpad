<% elem_id = "attachment" + @a.id.to_s %>
{
  "id" : "<%= elem_id %>",
  "remove_link": "<%= raw((link_to '×', attachment_path(@a),
  method: :delete, remote: true, id: elem_id, class: 'X18').gsub('"', "'") )%>",
  "link" : "<%= @a.file.url %>",
  "type": "<%= @a.type %>"
}

